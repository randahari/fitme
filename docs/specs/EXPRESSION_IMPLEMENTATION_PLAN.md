# EXPRESSION_IMPLEMENTATION_PLAN

## Engineering Handoff — Implementation Sequence for Expression (D3 §17's Sixth Collaborator)

**Status:** DONE — all fifteen Work Packages (WP1–WP15) implemented, reviewed, approved, and closed (Product Review, Architecture Review, and Engineering Review all approved for each — §3). Expression (D3 §17's sixth and final Coach Decision System collaborator) is fully implemented; see `EXPRESSION_SPEC_v1.0.md` §33.5 for the governing Closure Record.
**Authority:** Lead Engineer
**Canonical Status:** Non-Canonical — this document introduces no new authority, no new Product decisions, no new Architecture decisions, and no new Canonical decisions. It exists solely to record, for continuity across sessions, the Work Package sequence Engineering proposes for implementing the approved `EXPRESSION_SPEC_v1.0.md` (v1.2, accepted).
**Source of Truth:**
- `docs/specs/EXPRESSION_SPEC_v1.0.md` (the approved Specification, v1.2)
- `docs/engineering/FITME_ENGINEERING_WORKFLOW_v1.0.md` (the governing lifecycle and Git rules)
- `docs/specs/TASK_007_IMPLEMENTATION_PLAN.md`, `docs/specs/TASK_008_IMPLEMENTATION_PLAN.md` (the structural and disciplinary precedent this document follows)
**Purpose:** Engineering implementation sequencing only.

**Precedence statement:** This document never overrides `docs/specs/EXPRESSION_SPEC_v1.0.md`. If any conflict exists between this document and the Specification, the Specification always prevails. This document does not restate, reinterpret, or amend any normative requirement, any of CD-EXP-01 through CD-EXP-04, or any Open Decision Register item (Appendix C) — it only proposes the order and grouping in which Engineering intends to satisfy the Specification. It is not part of the Source-of-Truth hierarchy and requires Product/Architecture review of its own before any Work Package begins, since — unlike TASK-007/008 at their own planning stage — this Plan is presented *before*, not after, that review.

**Prerequisite gate, before any Work Package begins (not itself a Work Package) — RESOLVED / CLEARED, preserved here as historical record, not removed:** `EXPRESSION_SPEC_v1.0.md` §28/Appendix C `EXP-OD-5` recorded that the No-Touch list variance for `js/coachDecisionSystem/*` was deferred to Engineering Readiness Review, that Review occurred, and an explicit, confirmatory Architecture sign-off remained due but not yet given. That sign-off has now been given: Product and Architecture have explicitly confirmed that the inherited `js/coachDecisionSystem/*` restriction does not apply to Expression's own implementation, recording the application of existing approved architectural precedent, introducing no new Product or Architecture decision. `EXPRESSION_SPEC_v1.0.md` §28 and Appendix C `EXP-OD-5` reflect this resolution. **Implementation work inside `js/coachDecisionSystem/*` for Expression is accordingly authorized; the gate below no longer blocks any Work Package.**

---

## 1. Chapters Requiring No Dedicated Work Package

Recorded here for completeness — these Specification chapters require no engineering code change, for the reason stated, and are therefore not assigned to any Work Package below.

| Chapters | Reason no Work Package is needed |
|---|---|
| §1–§3, §5 | Document Control, Executive Summary, Canonical Foundation, and Terminology are definitional. Nothing to implement. |
| §4 | Scope/Non-Goals is a cross-cutting constraint every Work Package below must respect, not a deliverable of its own — verified at WP14's audit. |
| §7 | Product Objectives is philosophy/grounding, realized *through* every Work Package's own rendering behavior, not built independently. |
| §9 | Explicit Forbidden Responsibilities is a cross-cutting constraint (mirrors §4's treatment) — verified at WP14's audit. |
| §22 | Accessibility is a declarative non-obligation: Expression produces no UI (§22, EXP-46) — confirmed, not built, at WP12. |
| §24 | Cross-Platform and Native Compatibility is a declarative boundary statement — Expression requires no platform-specific logic by construction; confirmed at WP12, not built. |
| §25 | The Architecture Ownership Matrix is already complete inside the Specification itself. |
| §26 | Existing-System Baseline and Migration is reference material WP9 consults; it is not itself a coding task. |
| §27 | Repository Impact is a descriptive forecast, confirmed against actual touched files at WP14/WP15, not a deliverable of its own. |
| §28 | No-Touch and Protected Areas is the cross-cutting prerequisite gate stated above, not a Work Package. |
| §29, §30 | Verification/Test Strategy and Acceptance Criteria are evaluated against the cumulative result of WP1–WP13; each Work Package's own "Acceptance criteria" column below realizes its share. |
| §31 | Engineering Readiness Review is already complete — the Gate Review this Plan follows, not a build requirement. |
| §32 | The Traceability Matrix is already complete inside the Specification. |
| §33 | Documentation/Closure Requirements are realized at WP15 only. |
| §34 | Appendices are already complete inside the Specification. |

---

## 2. Proposed Work Package Plan

### Phase 1 — Foundation: Schema and Dispatch (resolves EXP-OD-9, EXP-OD-3, EXP-OD-7)

| ID | Objective | SPEC sections | Expected repository files | Dependencies | Expected tests | Acceptance criteria (SPEC §30) |
|---|---|---|---|---|---|---|
| **WP1** | Define the Delivery Intent's literal field-level schema (property names, types) realizing EXP-51 (rendered language), EXP-52 (structured semantic signals), EXP-53 (correlation metadata), per CD-EXP-01's three content categories and EXP-56's prohibited-content list — an Engineering Decision Pending (`EXP-OD-9`), resolvable by Engineering per the Specification's own classification, not requiring a fresh Product/Architecture Canonical Decision. Fix `schemaVersion` per the `memoryLayer.js` convention (§11.5, `TASK_006_SPEC_v1.0.md` §25.13). **Investigation gate:** review `js/coachDecisionSystem/decisionFormation.js`'s existing object-construction pattern (`freezeShallow`, immutability discipline) before proposing the shape, so Delivery Intent's construction matches the repository's own established convention rather than inventing a new one. | §11 (all), §21 | `js/coachDecisionSystem/` (new module or shared contract file — exact filename an Engineering choice, per `TASK_006_SPEC_v1.0.md` E-2 precedent) | None (Architecture prerequisite gate resolved, above) | New schema-conformance test asserting all three EXP-51/52/53 categories present and none of EXP-56/57's prohibited fields | AC-8, AC-9, AC-11 |
| **WP2** | Resolve the dispatch mechanism (`EXP-OD-3`) — whether Expression is invoked as an extension of `runDecisionPass()`'s own internal call chain or a separate function invoked after receiving its return value (§6, EXP-08) — and wire Expression as an internal collaborator of the single `coachDecisionSystem` Composite Engine (§17, EXP-40): no new B2 Engine Registry entry, no new trigger type. **Stop condition:** if the chosen mechanism would require any change to `js/engineRegistry.js` or `registerCoachDecisionSystem.js`, this Work Package stops and reports — that would contradict §17/EXP-40 and D3 §17 Decision 1, not an Engineering choice to make unilaterally. | §6, §17 | `js/coachDecisionSystem/internalPipelineOrchestrator.js` | WP1 (Expression's dispatch must return a schema-conformant Delivery Intent) | New dispatch-wiring test; extend `tests/coachDecisionSystemWiring.test.js`-equivalent single-registration assertion | AC-7 |

### Phase 2 — Core Rendering Behavior (resolves CD-EXP-02, CD-EXP-03, CD-EXP-04)

| ID | Objective | SPEC sections | Expected repository files | Dependencies | Expected tests | Acceptance criteria (SPEC §30) |
|---|---|---|---|---|---|---|
| **WP3** | Input handling: defensive `TerminalDecision` validation against `TASK_006_SPEC_v1.0.md` §25.1/§25.4/§25.5 before rendering (EXP-19); `SILENCE`-kind no-output behavior (EXP-29, EXP-50) for both the zero-Candidates and Safety-`DEFERRED` origin cases. A validation failure produces no Delivery Intent (§19 exceptional-flow table). **Re-sequencing note (approved Product/Architecture sequencing decision):** Pre-Expression User Correction withholding (EXP-13, `D2-EF-07`) is **not** part of WP3's own coded scope — its implementation responsibility is re-sequenced to WP9 (below), where the live Coach Runtime call path that makes the withholding guarantee necessary is first wired. This is a re-sequencing, not a reduction of WP3: WP3's own implemented scope (defensive validation, both Silence origins) is unaffected and complete. EXP-13 is satisfied through this re-sequencing, not left unsatisfied — no production code path currently exists through which a superseded `TerminalDecision` could reach `runExpressionStage()` at all (confirmed by direct repository inspection: no caller sequences `runDecisionPass()` → `runExpressionStage()` yet), so there is nothing for WP3's own module to withhold against today. The guarantee becomes a genuine requirement only once WP9 makes that call path live, per D2-EF-07/EXP-13's own accepted ownership analysis: Expression owns withholding by construction (never being invoked for a superseded pass), not by any detection logic of its own — see WP9's own entry for the mechanism. | §10, §13, §19 (validation row only) | `js/coachDecisionSystem/` (Expression module, from WP1/WP2) | WP1, WP2 | New input-contract validation tests; Silence no-output tests (both origin cases) | AC-1, AC-2 |
| **WP4** | Ordinary rendering (the base case): `RECOMMENDATION`/`INITIATIVE`-kind, `UNMODIFIED`-disposition Terminal Decisions — currently the *only* disposition value reachable in production (per the accepted architecture investigation) — rendered per §12's general principles (Coach Bible Ch.4 doctrine, `D1-ER-05` confidence-firmness calibration, D1 Unit 13 personalization to tone/framing only). **Investigation gate:** review `js/coach/coachPresenter.js`'s existing `deps.coachMessageFn(ctx)` dependency-injection pattern and its wiring (likely in `js/app.js`) for how it reaches `functions/index.js`'s `anthropicProxy` Cloud Function, before proposing Expression's own generative-call mechanism — reuse precedes invention, matching this repository's own established discipline (TASK-008 Decision 9), and Expression's own "performed by a generative/LLM layer" requirement (EXP-10) does not imply a *new* LLM-calling infrastructure. Language generation targets Hebrew only (§23, EXP-47). No phrasebook / no literal wording is fixed anywhere in this Work Package (EXP-28) — content quality is a rendering behavior, not a literal string match. **Implemented portion (Product Review: APPROVED; Architecture Review: APPROVED, implemented scope):** `js/coachDecisionSystem/expressionRenderer.js` + `tests/expressionRenderer.test.js` (18 tests) — base-case rendering dispatch, generative-call injection pattern, confidence-firmness calibration, EXP-27 prohibitions, Hebrew-only targeting, explicit rejection of out-of-WP4-scope kinds/dispositions/tied-sets, never-fabricate failure handling. **Architecture Decision now approved (Canonical Decision 8; D2 Unit 04 Stage 10 Amendment 1; D3 Decision 7):** `D1-PER-03`'s previously-unresolved signal-availability gap is resolved by a second, narrow, closed, Memory-Layer-produced Stage-10 input — the Expression Rendering Context, `{schemaVersion, relationshipMaturityStage}` (`EXPRESSION_SPEC_v1.0.md` §10.1, EXP-73–78) — not by modifying `TerminalDecision`. **WP4's remaining implementation work under this Architecture Decision was subsequently authorized, completed, and formally closed** — see the list below (now a completed record, not a pending one) and §3's own WP4 row for the closure. | §8, §10.1, §12, §23 | `js/coachDecisionSystem/` (Expression module; new `expressionRenderingContext.js` contract module; `memoryLayer.js` extended to produce it; `internalPipelineOrchestrator.js` extended to dispatch it) | WP1, WP2, WP3 | New rendering-model tests (tone/personalization bound checks, e.g. no fear/shame/guilt/urgency markers per EXP-27); confirm Hebrew-only output; new Expression Rendering Context contract/validation tests; `relationshipMaturityStage`/`UNKNOWN`-conservative consumption tests (EXP-76) | (contributes to AC-9's "rendered language present"); AC-22 (Expression Rendering Context — implemented, WP4) |

**WP4 remaining implementation work (subsequently authorized, completed, and formally closed — this list is now a completed record, per §3's own WP4 row):**
1. `js/coachDecisionSystem/expressionRenderingContext.js` — new contract module defining/validating the closed `{schemaVersion, relationshipMaturityStage}` shape (EXP-74/75/77), mirroring `deliveryIntentContract.js`'s existing structural-rejection discipline; no `immutable` payload field (immutability enforced structurally, e.g. `Object.freeze`, per the approved adjustment).
2. `js/coachDecisionSystem/memoryLayer.js` — extended to produce the Expression Rendering Context from its own already-computed `relationshipMaturity.stage` value (D3 Decision 3/7), as a bounded, separate output — Pipeline Context's own existing shape is unchanged.
3. `js/coachDecisionSystem/internalPipelineOrchestrator.js` — `runExpressionStage()` extended to accept and dispatch the Expression Rendering Context as a second parameter, with abort-rather-than-fabricate handling if it is missing/invalid, mirroring the existing `EXPRESSION_PORT_UNAVAILABLE` precedent.
4. Defensive validation of the Expression Rendering Context at Expression's own boundary (EXP-77), mirroring `expressionInputGate.js`'s existing EXP-19 discipline for `TerminalDecision`.
5. `js/coachDecisionSystem/expressionRenderer.js` — updated to consume `relationshipMaturityStage` and implement EXP-76's conservative-default scaling behavior (`UNKNOWN` treated at least as conservatively as Observer-stage scope, direct analogy to `TASK_005_SPEC_v1.0.md` §17.7/E-2); the existing defensive negative test (no `relationshipMaturity` leakage) is superseded, not merely extended — Expression is now authorized to reference this signal.
6. New `tests/expressionRenderingContext.test.js` (contract/validation, mirroring `tests/deliveryIntentContract.test.js`); `tests/expressionRenderer.test.js` updated for positive consumption + `UNKNOWN`-conservative behavior; `tests/internalPipelineOrchestrator.test.js` updated for the new dispatch parameter; `tests/coachDecisionSystemWiring.test.js` updated for the new module (module count, load order).
7. `index.html`/`sw.js` — new module script tag/SHELL entry, dependency-ordered.
8. Full regression suite re-run; WP4 formal closure request (Product/Architecture/Engineering Review) once the above is implemented and tested.
| **WP5** | `REFUSAL` rendering — Canonical Decision CD-EXP-02 (EXP-58–62): explicit non-fulfillment statement, never disguised as an ordinary recommendation; limitation attributed to the situation, never to the user; no blame/shame/failure/wrongdoing language; safer alternative presented only if supplied in the data received (the current, universal case: none is, per the accepted architecture investigation — this branch is not implemented as live logic beyond a defined no-op, since no upstream source supplies one), never fabricated or drawn attention to its absence; no comparison, measurement, or judgment of the user. | §14.2 | `js/coachDecisionSystem/` (Expression module) | WP1–WP4 | New `REFUSAL` rendering tests (one per EXP-58–62); see WP13 for the qualitative-rule verification mechanism these depend on | AC-12, AC-13, AC-14 |
| **WP6** | `ESCALATION` rendering — Canonical Decision CD-EXP-03 (EXP-63–68): rendered as an already-decided fact (no independent assessment of whether escalation is warranted); calm encouragement toward professional support, no diagnosis, no named condition; same register as ordinary coaching guidance (not alarm/dramatic — a single fixed register, since no Urgency/severity dimension reaches Expression); explicit affirmation that coaching continues; no statement or implication that FITME contacts, will contact, or replaces a healthcare provider or third party; no softening to the point seriousness could be missed. | §14.3 | `js/coachDecisionSystem/` (Expression module) | WP1–WP4 | New `ESCALATION` rendering tests (one per EXP-63–68) | AC-15, AC-16 |
| **WP7** | Safety-intervention disclosure — Canonical Decision CD-EXP-04 (EXP-69–72): applied to WP5/WP6's `BOUNDARY`-kind output and to a `MODIFIED`-disposition Terminal Decision if one is ever received (§14.5 remains unimplemented — see WP7's own scope note below), scoped exactly to `safetyDisposition.disposition ∈ {MODIFIED, BLOCKED, ESCALATED}`, explicitly excluding `DEFERRED` (no Delivery Intent, per WP3) and `UNMODIFIED` (nothing to disclose, per WP4). Acknowledgment woven into the same rendered message, never a separate technical notice; never names an internal component, stage, disposition value, or `reasonCode`. **Explicit scope boundary, restated, not redefined:** this Work Package implements disclosure only. It does not implement, and must not attempt to implement, the bounded-modification content-generation algorithm itself (§14.5, `EXP-OD-10`) — that remains open, no canonical source exists for it, and `MODIFIED` is currently unreachable in production; where a `MODIFIED` disposition is received, this Work Package renders `modification.modifiedContent` as already-authored content per §12/WP4 plus this disclosure acknowledgment, without generating or altering that content itself. | §14.4 (all); §14.5 (scope-exclusion only — no code) | `js/coachDecisionSystem/` (Expression module) | WP1–WP6 | New disclosure tests (one per EXP-69–72), including negative tests confirming no acknowledgment for `DEFERRED`/`UNMODIFIED` | AC-17, AC-18, AC-19 |
| **WP8** | Multi-option (tied-set) rendering (§15, EXP-35): render the full, already-assembled `options[]` array unmutated as a single Delivery Intent with multiple user-selectable options; a `MODIFIED` disposition on a tied-set decision applies at the whole-decision level only (`[SLDP RCD-15]`), never per-option — WP5/WP6/WP7 apply identically to a tied-set case as to a single-option one. | §15 | `js/coachDecisionSystem/` (Expression module) | WP1–WP7 | New tied-set rendering test; confirm `options[]` count/order/membership preserved unmutated | (contributes to AC-9; no dedicated AC beyond those already covering EXP-35's parent rules) |

### Phase 3 — Boundary and Exceptional-Flow Integration

| ID | Objective | SPEC sections | Expected repository files | Dependencies | Expected tests | Acceptance criteria (SPEC §30) |
|---|---|---|---|---|---|---|
| **WP9** | Resolve `EXP-OD-4` — which existing Coach Runtime entry point (`js/coach/coachPresenter.js`, `js/trigger/triggerController.js`, or a new function added to one of them) receives Expression's Delivery Intent, and wire the hand-off (§16, §26, EXP-36–39, EXP-49). **Investigation gate:** confirm at this Work Package's own start, by direct inspection, that neither file has begun referencing `js/coachDecisionSystem/*` since the Specification's own authoring baseline (`TASK_007_SPEC_v1.0.md` §13.4's verified-absent finding). **Stop condition (delivery surface):** whether a new delivery surface is created is not open (D3 §17 Decision 6 forecloses it, §16) — if the chosen wiring would require anything resembling a new surface, this Work Package stops and reports. No existing chat/Trigger-card behavior may be altered as a side effect. **Mandatory prerequisite — D2-EF-07 supersession guarantee (re-sequenced from WP3, per the approved Product/Architecture sequencing decision):** live Coach Runtime wiring MUST NOT be considered complete, and this Work Package MUST NOT be marked closed, until the Pre-Expression User Correction supersession guarantee (EXP-13, `D2-EF-07`) is implemented and tested. The Engineering direction is fixed by the accepted investigation and MAY NOT be redesigned within this Work Package: freshness/correction-arrival state originates within the existing Memory Layer / Decision-Input-intake ownership boundary (D3 Decision 3, §11.1) — not a new component; the Internal Pipeline Orchestrator performs a fresh pre-dispatch supersession check, immediately before invoking `runExpressionStage()`, using its already-approved Stage-sequencing authority (D3 §11.1) — not a new orchestration authority; a superseded Decision Pass results in `runExpressionStage()` never being invoked — Expression itself receives no new input, no new field, and performs no detection of its own (EXP-13 is satisfied by construction); `TerminalDecision` is not modified in any way. **Stop condition (supersession guarantee):** if implementing the freshness/correction-arrival signal is found, at implementation time, to require new Product/Architecture authority — a new architectural component, a new Canonical Decision, or any change to the `TerminalDecision` contract — beyond what the accepted investigation already authorizes, this Work Package stops and reports rather than proceeding; the mechanism is authorized only in the form already accepted, not as a general license to redesign correction handling. | §16, §26, §9 EXP-13, §13, `D2-EF-07` | `js/coach/coachPresenter.js` and/or `js/trigger/triggerController.js` (exact target confirmed by the investigation gate, not assumed here); `js/coachDecisionSystem/internalPipelineOrchestrator.js` (pre-dispatch supersession check); the Memory Layer module (freshness/correction-arrival state, within its existing Decision-Input-intake ownership) | WP1–WP8 | Coach Runtime handoff tests: no reach-back into the Decision Engine, no delivery-failure feedback; extend the target file's own existing test suite; new supersession-guarantee tests: a Pre-Expression correction results in `runExpressionStage()` not being invoked; a Post-Expression correction is unaffected (`D2-EF-07`) | AC-3, AC-10 |
| **WP10** | Exceptional flows (§19): `TerminalDecision` validation-failure handling (extends WP3); generative/LLM-layer call failure, contained per the `recommendationEngine.js`/`initiativeEngine.js` "never throws" precedent; Coach Runtime unavailability handled as downstream/not Expression's concern (per WP9's boundary). No fabricated Candidate, Terminal Decision, or Delivery Intent as fallback content in any case (EXP-42). | §19 | `js/coachDecisionSystem/` (Expression module) | WP3, WP9 | New failure-path tests, one per §19's exceptional-flow table row | (no dedicated AC; verifies the no-fabrication discipline underlying AC-1/AC-2/AC-9) |

### Phase 4 — Cross-Cutting Confirmation

| ID | Objective | SPEC sections | Expected repository files | Dependencies | Expected tests | Acceptance criteria (SPEC §30) |
|---|---|---|---|---|---|---|
| **WP11** | Memory/Persistence boundary confirmation (§18, EXP-41): confirm, by direct code inspection and a negative test, that the completed Expression module performs zero calls to `js/persistenceGateway.js` and holds no StateAccess capability of its own — by direct structural analogy to the Decision Engine and Safety Layer's own already-confirmed absence of one. Audit only; no code expected. | §18 | None expected (audit) | WP1–WP10 | New negative test: zero `persistenceGateway`/`stateAccess` write-capability references in the Expression module | AC-5 |
| **WP12** | Determinism, Explainability, Accessibility, Language, and Cross-Platform confirmation (§20–§24): confirm decision-reaching determinism (kind-level outcome identical across runs; generated language not asserted identical, EXP-43); confirm the async/sync execution model chosen at WP2 matches the B2 async contract without further Architecture Decision (EXP-44); confirm correlation metadata (WP1) preserves traceability without duplicating `TerminalDecision` (EXP-45, EXP-53); confirm no accessibility, RTL/layout, or native-platform code was introduced anywhere in WP1–WP10 (Expression produces no UI by construction). Audit only. | §20, §21, §22, §23, §24 | None expected (audit); corrections only if the audit finds a genuine gap | WP1–WP10 | New determinism test (identical `TerminalDecision` → identical kind-level outcome); correlation-metadata no-duplication test | AC-6, AC-11 |

### Phase 5 — Verification and Closure

| ID | Objective | SPEC sections | Expected repository files | Dependencies | Expected tests | Acceptance criteria (SPEC §30) |
|---|---|---|---|---|---|---|
| **WP13** | Resolve `EXP-OD-11` — the verification mechanism for CD-EXP-02/03/04's qualitative content-judgment rules (EXP-59, EXP-62, EXP-65, EXP-70's "coach's own voice"), which are not mechanically checkable field/absence assertions. Design and implement a deterministic test-double approach, by direct precedent of `tests/fixtures/safetyIntegrationPortTestDouble.js` (TASK-006) — a deterministic double standing in for Expression's own generative/LLM layer during WP5/WP6/WP7's own test suites, never production-reachable. Retrofit WP5/WP6/WP7's test files to use it where they currently assume live generation. | §29 (test-double discipline); realizes `EXP-OD-11` | `tests/fixtures/` (new Expression test double, exact filename an Engineering choice) | WP5, WP6, WP7 | New test-double conformance test confirming production code never imports/references it (mirroring `safetyIntegrationPortTestDouble.js`'s own negative test) | (no dedicated AC; closes `EXP-OD-11`, supports AC-12–19's own test infrastructure) |
| **WP14** | Cross-cutting audit: confirm every remaining normative rule not covered by WP1–WP13 is satisfied, with cited evidence; confirm no file outside `js/coachDecisionSystem/*` (the one confirmed variance, prerequisite gate above) was touched by any prior Work Package — the rest of §28's inherited No-Touch list (`js/engineRegistry.js`, `js/stateAccess.js`, `js/persistenceGateway.js`, `firestore.rules`, any Product Bible/Coach Bible/AI Constitution/D1/D2/D3 content) honored exactly; confirm every Acceptance Criterion in §30 is satisfied by the cumulative result, with AC-8 through AC-19 individually re-verified; confirm `EXP-OD-2` through `EXP-OD-11` remain correctly recorded (resolved items marked resolved, non-blocking items still non-blocking, `EXP-OD-10` still explicitly unimplemented). | §29 (remainder), §30 (remainder) | Determined by audit findings only; expected to be zero or near-zero file changes beyond fixing a genuine, narrow gap the audit reveals | WP1–WP13 all complete and approved | Any test additions are audit-finding-driven only | All of AC-1–AC-19 (re-verification, not first implementation) |
| **WP15** | Documentation and closure: Roadmap and Changelog Expression entries (matching the TASK-007/TASK-008 closure format); `EXPRESSION_SPEC_v1.0.md`'s own status-header transition to DONE/CLOSED and Closure Record (§33.5); `docs/architecture/FITME_ARCHITECTURE_v1.md` synchronization, following the existing §21–§24 pattern (TASK-006 added §23, SL-001 added §24) — a new entry documenting Expression's realization as the sixth and final D3 §17 collaborator; resolve `EXP-OD-6` (`APP_VERSION` advancement) based on WP9's actual outcome — advance only if WP9's Coach Runtime wiring ships real, shipped user-visible behavior, per §27's own conditional, not assumed either way by this Plan; final full-regression confirmation; the request for Final Product Verification / Final Architecture Verification. `EXP-OD-10` (bounded-modification content-generation algorithm) remains explicitly open at closure — not resolved, not silently dropped, carried forward as Expression's own tracked follow-up, exactly as SL-001, TASK-006, and every other closed task in this repository has carried forward its own non-blocking open items. | §33 | `docs/roadmap/Roadmap.md`, `docs/roadmap/Changelog.md`, `docs/specs/EXPRESSION_SPEC_v1.0.md` (Closure Record section only — no normative content altered), `docs/architecture/FITME_ARCHITECTURE_v1.md` (new section, additive only) | WP1–WP14 all complete and approved | Full `node --test tests/*.test.js` run only | AC-20, AC-21 |

**Coverage confirmation:** every one of the 34 Specification chapters is accounted for — either inside a Work Package above or in §1's "no dedicated Work Package" table with a cited reason. Every Acceptance Criterion (AC-1 through AC-21) is mapped to exactly one Work Package's own verification. `EXP-OD-3`, `EXP-OD-4`, `EXP-OD-9`, and `EXP-OD-11` are resolved by WP2, WP9, WP1, and WP13 respectively, each within Engineering's own already-classified authority (Engineering Decision Pending), introducing no new Product or Architecture decision. `EXP-OD-5` was a precondition gate, not a Work Package — it is now RESOLVED/CLEARED by the official Product/Architecture confirmation recorded in `EXPRESSION_SPEC_v1.0.md` §28/Appendix C, not by this Plan itself. `EXP-OD-6` remains contingent on WP9's actual outcome, resolved no earlier than WP15. `EXP-OD-10` remains explicitly, permanently out of this Plan's scope. EXP-13 (`D2-EF-07` Pre-Expression User Correction withholding) is re-sequenced from WP3 to WP9 by an approved Product/Architecture sequencing decision — its own AC (AC-3) remains, unchanged, mapped to WP9 as it already was; this is a change in which Work Package implements the guarantee, not a change to AC coverage or to §11.1's ownership assignment.

---

## 3. Implementation Tracking

| WP | Status | Commit hash | Product Review | Architecture Review | Engineering Review | Notes |
|---|---|---|---|---|---|---|
| WP1 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | `deliveryIntentContract.js` + `tests/deliveryIntentContract.test.js`. `EXP-OD-5` prerequisite gate RESOLVED/CLEARED (above). Product Review, Architecture Review, and Canonical Review all APPROVED; WP1 officially CLOSED. |
| WP2 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | `runExpressionStage()` dispatch added to `internalPipelineOrchestrator.js`, as a separate function (EXP-OD-3 resolved) preserving `runDecisionPass()`'s existing contract unchanged; `tests/internalPipelineOrchestrator.test.js` extended. Product Review, Architecture Review, and Canonical Review all APPROVED; WP2 officially CLOSED. |
| WP3 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | `expressionInputGate.js` (defensive `TerminalDecision` validation, EXP-19; Silence-kind no-output, EXP-29/EXP-50, both origins) + `tests/expressionInputGate.test.js`; `internalPipelineOrchestrator.js`/`internalPipelineOrchestrator.test.js` extended accordingly. Pre-Expression User Correction withholding (EXP-13) re-sequenced to WP9 by approved Product/Architecture sequencing decision, prior to this closure — not a WP3 implementation gap (see §2 WP3 entry). Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. WP3 officially CLOSED. |
| WP4 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | `expressionRenderer.js`/`expressionRenderer.test.js` (base-case rendering) implemented and approved. Architecture Decision (Expression Rendering Context, Canonical Decision 8; D3 Decision 7) approved, then its own remaining implementation work completed: `expressionRenderingContext.js` (new contract module, EXP-74/75/77); `memoryLayer.js` extended to produce it from `relationshipMaturity.stage`; `internalPipelineOrchestrator.js`'s `runExpressionStage()` extended to accept and dispatch it, with `EXPRESSION_RENDERING_CONTEXT_REJECTED`/`INVALID_EXPRESSION_RENDERING_CONTEXT` abort-rather-than-fabricate handling; `expressionRenderer.js` updated to consume `relationshipMaturityStage` and implement EXP-76's `UNKNOWN`-conservative scaling; `tests/expressionRenderingContext.test.js` (new), `tests/expressionRenderer.test.js`/`tests/internalPipelineOrchestrator.test.js`/`tests/coachDecisionSystemWiring.test.js` extended; `index.html`/`sw.js` wired in dependency order. This closure formally records work verified as already complete and relied upon throughout WP9–WP14's own audits (Expression Rendering Context dispatch, `relationshipMaturityStage` consumption, and module presence were each independently re-confirmed there) — no new code was written to close this entry; a WP15 procedural-accuracy correction, per explicit Product/Architecture authorization. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. WP4 officially CLOSED. |
| WP5 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | `REFUSAL` rendering (`BOUNDARY`-kind, `boundaryType: 'REFUSAL'`, `BLOCKED` disposition), per Canonical Decision CD-EXP-02 (EXP-58–62), implemented in `js/coachDecisionSystem/expressionRenderer.js` (its own header comment records this Work Package as CLOSED) and `tests/expressionRenderer.test.js`. Explicit non-fulfillment statement, never disguised as an ordinary recommendation; limitation attributed to the situation, never the user; no blame/shame/failure/wrongdoing language; safer alternative presented only if supplied in the data received. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. WP5 officially CLOSED. |
| WP6 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | `ESCALATION` rendering (`BOUNDARY`-kind, `boundaryType: 'ESCALATION'`, `ESCALATED` disposition), per Canonical Decision CD-EXP-03 (EXP-63–68), implemented in `js/coachDecisionSystem/expressionRenderer.js` (its own header comment records this Work Package as CLOSED) and `tests/expressionRenderer.test.js`. Rendered as an already-decided fact; calm encouragement toward professional support, no diagnosis, no named condition; same register as ordinary coaching guidance; explicit affirmation that coaching continues; no statement or implication that FITME contacts/will contact/replaces a healthcare provider or third party. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. WP6 officially CLOSED. |
| WP7 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | Safety-intervention disclosure (CD-EXP-04, EXP-69–72), applied unconditionally to every `REFUSAL`/`ESCALATION` rendering (via the shared `DISCLOSURE_ACKNOWLEDGMENT_LINE` instruction fragment) and to a new `MODIFIED`-disposition rendering path (`modification.modifiedContent` rendered per §12/WP4's own general principles plus the same disclosure acknowledgment, without this module generating, inferring, altering, or repairing that content — `EXP-OD-10`, the bounded-modification content-generation algorithm itself, remains explicitly open and unaddressed by this module). Implemented in `js/coachDecisionSystem/expressionRenderer.js` (its own header comment records this Work Package as CLOSED) and `tests/expressionRenderer.test.js`. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. WP7 officially CLOSED. |
| WP8 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | Multi-option (tied-set) rendering (§15, EXP-35): every one of WP4's/WP5's/WP6's/WP7's four rendering paths governs a tied-set `TerminalDecision` (`options[]` present) exactly as it governs a single-option one; `options[]` preserved unmutated in count, order, and membership; a `MODIFIED` disposition on a tied-set decision applies `modification.modifiedContent` at the whole-decision level only (`[SLDP RCD-15]`), never per-option. Implemented in `js/coachDecisionSystem/expressionRenderer.js` (its own header comment records this Work Package as CLOSED) and `tests/expressionRenderer.test.js`. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. WP8 officially CLOSED. |
| WP9 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | Coach Runtime handoff wired: `internalPipelineOrchestrator.js`'s `run()` now performs the full live Stage 1→10 sequence, with `SafetyLayer` (SL-001) and `ExpressionRenderer` injected as production `safetyPort`/`expressionPort` (treated as ordinary Engineering integration within the already-approved architecture, per the accepted post-SL-001 re-validation of the historical TASK-006 §38 G-6 classification — G-6 withdrawn as a current blocker). `TriggerController.presentDeliveryIntent()` added, reusing the existing `#trigger-card` element — no new delivery surface (D3 Decision 6 preserved). D2-EF-07 Pre-Expression User Correction supersession guarantee implemented exactly per the fixed accepted direction: `memoryLayer.js` owns the freshness/correction-arrival state (`recordExplicitUserStatementArrival()`/`getExplicitUserStatementArrivalTimestamp()`), `run()` performs the pre-dispatch supersession check, a superseded pass never calls `runExpressionStage()`, `TerminalDecision` is unmodified. `tests/memoryLayer.test.js`, `tests/triggerController.test.js`, `tests/internalPipelineOrchestrator.test.js`, `tests/coachDecisionSystemWiring.test.js` extended; `tests/c1Wp5aWiring.test.js` updated (unrelated pre-existing call-site inventory, legitimately incremented by `ExpressionRenderer`'s `generateFn` reuse of `callClaude`). Two post-implementation investigations — the D2-EF-07 write-side (no live chat-input UI exists; mechanism correct but currently dormant, closely related to already-tracked Repository Gap G-2) and `presentDeliveryIntent()`/`presentTriggerCard()` `#trigger-card` coexistence (dormant given Repository Gap G-2; the live precedence question already falls under TASK-007's own pre-existing, still-unresolved OD-5/UX-12.6 Product Decision Pending) — were both accepted as currently dormant and outside WP9's closure criteria. Full regression: 1768/1768. Product Review: APPROVED. Architecture Review: APPROVED. Engineering Review: PASSED. WP9 officially CLOSED. |
| WP10 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | Exceptional flows (§19) confirmed: investigation gate found all three exceptional-flow table rows already correctly, defensively implemented in `runExpressionStage()` as a byproduct of WP2/WP3/WP4's own scope — no production code change required. Row 1 (`TerminalDecision` validation failure, extends WP3) → `ABORTED / INVALID_TERMINAL_DECISION`. Row 2 (generative/LLM-layer call failure) → `try/catch` around `expressionPort.render()` → `ABORTED / EXPRESSION_RENDER_THREW`, matching `recommendationEngine.js`'s/`initiativeEngine.js`'s "never throws" precedent. Row 3 (Coach Runtime unavailable) → true by construction: `runExpressionStage()` returns before `js/app.js`'s `presentDeliveryIntent()` call site (WP9) is ever reached; structurally confirmed by a new negative test that `internalPipelineOrchestrator.js` contains no reference to `triggerController`/`coachPresenter`/`presentDeliveryIntent`. `tests/internalPipelineOrchestrator.test.js` extended with 4 new tests (one per §19 table row, plus one consolidated EXP-42 no-fabrication confirmation across all five existing abort reasons). Full regression: 1772/1772. Product Review: APPROVED. Architecture Review: APPROVED. Engineering Review: PASSED. WP10 officially CLOSED. |
| WP11 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | Memory/Persistence boundary confirmation (§18, EXP-41; AC-5) — audit only, no production code change. Direct inspection confirmed the Expression module's four dedicated files (`deliveryIntentContract.js`, `expressionInputGate.js`, `expressionRenderingContext.js`, `expressionRenderer.js`) already have zero `persistenceGateway`/`StateAccess` usage, by direct structural analogy to the Decision Engine (`TASK_006_SPEC_v1.0.md` §29.3) and Safety Layer (`SL-001_SPEC_v1.0.md` §20) precedents. Verified this coverage did not already exist: `tests/coachDecisionSystemWiring.test.js`'s prior PersistenceGateway/StateAccess checks (its own tests 14/16) covered `recommendationEngine.js`/`initiativeEngine.js`/`internalPipelineOrchestrator.js`/`memoryLayer.js`/the five TASK-006 modules only, never the four Expression-specific files. `tests/coachDecisionSystemWiring.test.js` extended with 2 new negative tests (zero PersistenceGateway calls; zero StateAccess capability). Full regression: 1774/1774. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. Engineering Review: PASSED. WP11 officially CLOSED. |
| WP12 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | Determinism, Explainability, Accessibility, Language, and Cross-Platform confirmation (§20–§24; AC-6, AC-11) — audit, plus one closed gap. Investigation found WP4's own `expressionRenderer.test.js` #69 already covered `ExpressionRenderer.render()`'s own kind-level determinism (AC-6/EXP-43), and `deliveryIntentContract.test.js` (WP1) already fully covered correlation no-duplication (AC-11/EXP-53, schema-locked to `['decisionId']`, extra fields rejected) — cited, not duplicated. Closed the one genuine residual gap: `runExpressionStage()`'s own orchestrator-level dispatch-status determinism (`DISPATCHED`/`ABORTED`/`NO_DELIVERY_INTENT` identical across repeated invocation) had no dedicated test — added `tests/internalPipelineOrchestrator.test.js` #43. Added `tests/coachDecisionSystemWiring.test.js` #28 (accessibility/RTL/native-platform code absence in the Expression module's four files, §21/§22/§24 — the pre-existing `window.X = API` dual-export convention correctly not flagged) and #29 (EXP-44 async/await execution-model confirmation). No production code changed. Full regression: 1777/1777. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. Engineering Review: PASSED. WP12 officially CLOSED. |
| WP13 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | Resolved `EXP-OD-11` — the deterministic verification mechanism for CD-EXP-02/CD-EXP-03/CD-EXP-04's qualitative content-judgment rules. Confirmed WP5/WP6/WP7's existing tests verified only the composed system instruction, never actual rendered output; built `tests/fixtures/expressionQualitativeVerificationTestDouble.js` — a deterministic, Hebrew keyword/pattern-based checker (six pure predicates: blame language EXP-59/60, comparison language EXP-62, diagnosis language EXP-64, alarm register EXP-65, third-party-contact implication EXP-67, internal-implementation-detail leakage EXP-71), by direct structural analogy to `safetyIntegrationPortTestDouble.js` (CD-T006-05) — test-only, never production-reachable (new negative test, `tests/coachDecisionSystemWiring.test.js` #30). Its own header states plainly it is a heuristic, never run against real production-generated output, and does not guarantee real generated content will comply. `tests/expressionQualitativeVerificationTestDouble.test.js` (13 conformance tests, new file) proves each checker discriminates a violating example from a compliant one; `tests/expressionRenderer.test.js` retrofitted with 3 new tests (70-72) applying the checker to actual REFUSAL/ESCALATION/MODIFIED rendered wording. No production code changed. Full regression: 1794/1794. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. Engineering Review: PASSED. WP13 officially CLOSED. |
| WP14 | ✅ Implemented | — (not yet committed) | APPROVED | APPROVED | PASSED | Cross-cutting audit. Closed two narrow AC coverage gaps found at Pre-Flight: `tests/coachDecisionSystemWiring.test.js` #31 (AC-3, no Decision Engine reach-back from the Expression module) and #32 (AC-4, no chat/trigger/UI/delivery-surface dependency, extending the existing #16 check — previously scoped only to the older TASK-004/005/006 modules — to the Expression module's own four files; required comment-stripping to avoid a false positive on legitimate investigation-gate citations). Synchronized `EXPRESSION_SPEC_v1.0.md` Appendix C: EXP-OD-3 (WP2), EXP-OD-4 (WP9), and EXP-OD-11 (WP13) corrected from stale "Engineering Decision Pending" to Resolved, each with its concrete resolution cited, preserving audit history — no normative rule, AC text, or Canonical Decision altered. Confirmed EXP-OD-10 unchanged (Product Decision Pending, non-blocking) and no No-Touch violation across WP1–WP14. No production code changed. Full regression: 1796/1796. Product Review: APPROVED. Architecture Review: APPROVED. Canonical Review: PASSED. Engineering Review: PASSED. WP14 officially CLOSED. |
| WP15 | ⏳ Not Started | — | — | — | — | |

---

## 4. Governing Constraints (Restated, Not Redefined)

The following restrictions apply to every Work Package and are restated here only for continuity — none of them originates in this document:

- The `EXP-OD-5` prerequisite gate (above) has received its explicit, confirmatory Architecture sign-off and is RESOLVED/CLEARED. Work Packages remain gated on their own individual Product/Architecture approval, per the next bullet.
- Each Work Package is implemented, tested, self-reviewed, and only committed after Product/Architecture approval of that package (C1/TASK-007/TASK-008 precedent, adopted here).
- `docs/roadmap/Roadmap.md`, `docs/roadmap/Changelog.md`, and the Specification's own Closure Record are not updated until WP15.
- No Work Package may introduce a new Runtime, Engine, delivery surface, or top-level architectural component; adopt an external dependency; change D1/D2/D3 ownership or reopen Decisions 1/5/6; modify the `TerminalDecision` contract, Safety Layer behavior, `reasonCode`/`reasonDetail`/`boundaryType`, or the Safety Decision Matrix; or reopen, narrow, or widen CD-EXP-01/02/03/04.
- No Work Package may resolve `EXP-OD-10` (the bounded-modification content-generation algorithm) by inference, interpretation, or unilateral Engineering choice.
- No Work Package may touch any file outside `js/coachDecisionSystem/*` (the one confirmed variance, once given) and the specific Coach Runtime file WP9's investigation gate identifies — every other item on §28's inherited No-Touch list remains fully protected.
- Where a Work Package's own stop condition or investigation gate is triggered, that Work Package stops and reports rather than proceeding on inference.
- Full regression (`node --test tests/*.test.js`) must remain at or above the 1607-test baseline after every Work Package.
- **Canonical Decision 8 — Expression Rendering Context (approved Architecture Decision, D2 Unit 04 Stage 10 Amendment 1; D3 Decision 7).** Expression receives a second, narrow, closed, Memory-Layer-produced Stage-10 input, `{schemaVersion, relationshipMaturityStage}` (`EXPRESSION_SPEC_v1.0.md` §10.1, EXP-73–78), resolving `D1-PER-03`'s signal-availability gap without modifying `TerminalDecision`. This was itself the authorization for WP4's remaining implementation work to touch `memoryLayer.js` and `internalPipelineOrchestrator.js` in the specific, narrow way §2's WP4 entry lists — not a general license to modify either file further. That remaining work was subsequently authorized, completed, and formally closed (§3's own WP4 row).
- **D2-EF-07 supersession sequencing (approved Product/Architecture sequencing decision).** The Pre-Expression User Correction supersession guarantee (EXP-13) is implementation responsibility of WP9, not WP3 — a re-sequencing, not a new Work Package. Its accepted Engineering direction is fixed and MAY NOT be redesigned by any Work Package: freshness/correction-arrival state originates within the Memory Layer's already-approved Decision-Input-intake ownership (D3 Decision 3); the Internal Pipeline Orchestrator performs the pre-dispatch supersession check using its already-approved Stage-sequencing authority (D3 §11.1); a superseded Decision Pass simply never reaches `runExpressionStage()`; Expression receives no new input and performs no detection; `TerminalDecision` is not modified. This introduces no new architectural component and no new Canonical Decision. If implementing this signal is found to require either, the Work Package attempting it stops and reports rather than proceeding.

---

## 5. Update Discipline

This document is updated after each Work Package's approval — its own §3 tracking-table row moves from "Not Started" to "✅ Implemented" with the resulting commit hash and review sign-offs — and is not otherwise revised. It does not accumulate narrative history; that remains the Specification's own Closure Record's role at WP15. If this document and `EXPRESSION_SPEC_v1.0.md` ever appear to disagree, the Specification governs, per its precedence statement above.

---

*End of Implementation Plan. Not yet approved. No Work Package has begun.*
